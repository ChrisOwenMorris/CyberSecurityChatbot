<Window x:Class="CyberSecurityChatbotUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberSecurity Chatbot" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="BotOutput"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Margin="10,10,10,100" />

        <TextBox x:Name="UserInput"
                 Height="30"
                 Margin="10,347,10,0"
                 VerticalAlignment="Top"
                 KeyDown="UserInput_KeyDown" />

        <Button Content="Send"
                Margin="10,389,10,0"
                VerticalAlignment="Top"
                Height="30"
                Click="SendButton_Click" RenderTransformOrigin="0.5,0.5" >
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="0.19"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>
    </Grid>
</Window>
